<!-- <html>
    <head>
        <title>
            <%= title %>
        </title>
    </head>
    <body> -->
<!-- including header and footer from another file -->
<!-- here _ is awritten to just specify is differnt from other files -->
<!-- <%- include('_header'); -%> -->


<!-- rest part is not needed -->
<link rel="stylesheet" href="/css/home.css">


<div id="home-container">

    <section id="feed-post">
        <!-- add a check if user is  not authenticated dont show form -->
        <h4>Posts</h4>
        <% if ( locals.user ){%>
            
            <form action="/posts/create" id="new-post-form" method="POST">
                <textarea id="form-input" name="content" cols="30" rows="3" placeholder="Type Something...." required></textarea>
                <input type="submit" value="Post">
            </form>
            <%}%>



                <div id="post-list-container">
                    <ul>
                        <% for( post of posts ) { %>

                            <!-- including partials -->
                            <%- include('_post') -%>

                                <% } %>
                    </ul>
                </div>
    </section>
    <section id="user-friends">
        <div id="people">
            <h4>People</h4>
        <% for(u of all_users) { %>
            <p>
                <a href="/users/profile/<%= u.id %>">
                    <%= u.name %>
                </a>
            </p>
            <% } %>
        </div>
        <div id="friends">
            <h4>Friends</h4>
        <% for(u of friends){%>
            <p><a href="/users/profile/<%=u.to_user.id%>"><%=u.to_user.name %></a></p>
        <%}%>
        </div> 
    </section>


</div>
<!-- importing this scripts for creating the comments  -->
<script src="/js/home_post_comments.js"></script>
<script src="/js/home_posts.js"></script>

<!-- CHANGE :: import the script file for toggle likes, also run it over for the already present posts and comments on the page -->
<script src="js/toggle_likes.js"></script>
<script>
    $('.toggle-like-button').each(function(){
        let self = this;
        let toggleLike = new ToggleLike(self);
    });
</script>

<!-- for js  -->
<!-- <script src="./css/js"></script> -->
<!-- <%- include('_footer'); -%>
    </body>
</html> -->